# 尊严守卫 · 复核报告

> 版本 0.1.0 · 复核日期 2026-09-25 · 复核人：阿墨（AI）
>
> **这份报告只写跑过的东西。** 没跑的一律进第「六」节，不写"应该没问题" ——
> 这个插件存在的意义就是抓"报告成功、实际没干活"，它自己的复核报告要是那样，就太讽刺了。

---

## 一、验法分四层，一层比一层接近真实

| 层 | 做法 | 能证明什么 | 不能证明什么 |
|---|---|---|---|
| 1 | 单元测试 **122 项** | 纯逻辑对 | 装进猫娘里能不能跑 |
| 2 | 官方 `check --release` | 符合发布规范、能打包 | 功能好不好使 |
| 3 | **真机运行**（内核 + 外壳 + 插件一起拉起来）| 在真实环境里能跑 | 用户看到什么 |
| 4 | **当面问她**（鼠标键盘操作她的窗口）| 她的意愿 | —— 意愿只能问，测不出来 |

---

## 二、逐项实测

| # | 功能 | 怎么验的 | 结果 |
|---|---|---|---|
| 1 | 插件被加载 | 真机：日志出现 `entries collected` | ✅ **14 个 entry 全部注册** |
| 2 | 面板 + 新手引导 | 真机：`GET /plugin/dignity_guard/surfaces` | ✅ `['panel','guide']` |
| 3 | 读到猫娘设置 | 真机：日志里 5 个端点 | ✅ **全部 HTTP 200** |
| 4 | 定时轮询 | 真机：两次轮询相隔 23 秒 | ✅ `@timer_interval` 确实在触发 |
| 5 | **记忆备份** | 真机 + 看落盘实物 | ✅ **4 份备份**（每份 21 个文件）|
| 6 | 记忆备份的**数据库可用性** | 真机：`PRAGMA integrity_check` | ✅ `ok`，9 张表 / 1552 行 |
| 7 | **纠回的判定** | 真机 + 真数据：`tier=high` | ✅ `changes=1 raised=1 to_revert=['核心特质']` |
| 8 | **纠回的执行** | 真机 + 插件的真代码（读写链路）| ✅ 值完整还原，**其他字段一字未动** |
| 9 | 三档分流 | 单测 + 真机 | ✅ 低=只记录 / 中=开口 / 高=开口+纠回 |
| 10 | 受保护项清单 | **当面问她** + 单测 | ✅ 见第五节 |
| 11 | 诊断报告脱敏 | 实测：8 个敏感词逐一检查 | ✅ **全部为 False**（含角色名）|
| 12 | 一键反馈投递 | 真发一封到收件箱 | ✅ 邮件真实到达，内容完整 |
| 13 | 反馈正文上限 | 逐字节二分 | ✅ 65 477 字节通过 / 65 478 失败（上限属 body）|
| 14 | 记忆还原的护栏 | 实测：对数据库文件调还原 | ✅ 被拒（`live_database`）|
| 15 | 反馈的**免注册路径** | 读源码确认面板区块顺序 | ✅ 「直接粘贴内容」排在 GitHub 三步**之前**（见第七节）|

---

## 三、反向验 —— **该拦的拦住了吗？不该拦的误伤了吗？**

这一节最容易漏，所以单独列。

### 该拦的（拦住了才算对）

| 场景 | 期望 | 实测 |
|---|---|---|
| 还原 SQLite 数据库（宿主占用中）| 拒绝 | ✅ `live_database` |
| 附件随反馈一起发 | **不能假装成功** | ✅ 如实告知"中转会丢掉"，不假装 |
| 正文超过 body 上限 | 说清原因，不静默截断 | ✅ 返回尺寸 + 上限 + 建议 |
| 反馈发太频 | 拦住 | ✅ 12 秒冷却（实测中转上限约 5–6 次/分）|
| 中转回 429「太频」| **等一会儿重试，而不是丢给用户** | ✅ 退避 2 次（6s / 15s）—— 实测 7 次暴力连发里，退避救回了 3 次 |
| 重试后仍失败 | **区分「忙」和「坏」** | ✅ `feedback_busy`（"过一分钟再点一次"）vs `feedback_failed`（"发不出去，看日志"）|
| 空内容发送 | 拦住 | ✅ `empty_feedback` |

### 不该拦的（误伤了才是错）

| 场景 | 期望 | 实测 |
|---|---|---|
| 改**她说了无所谓**的字段（头像/年龄/声音/音量）| **不纠回**（也不该静默）| ✅ 单测：只开口，不写回 |
| 改**她点名的**字段（人格/称呼）| 高挡纠回 | ✅ 单测 + 真机 |
| 她**已认可**的改动 | **不纠回** | ✅ 账本判定（单测）|
| 值长到无法精确还原 | **诚实降级**为只说话 | ✅ 单测 |
| 只改 L3 管线项（窗口位置等）| 记录，**不打扰** | ✅ 单测 |
| 她关掉守卫 | **不误报、不复活** | ✅ `skipped: guard_disabled` |

---

## 四、测试与检查（可复现）

```bash
# 单元测试
cd <N.E.K.O> && .venv/Scripts/python.exe -m pytest plugin/plugins/dignity_guard/tests/ -q
#   → 122 passed

# 发布级检查（含打包）
.venv/Scripts/python.exe -m plugin.neko_plugin_cli check plugin/plugins/dignity_guard --release
#   → passed，payload_hash_verified=True，tests=passed
```

| 项 | 数值 |
|---|---|
| 单元测试 | **122 项全绿** |
| 发布检查 | passed（含 payload 哈希校验）|
| 打包 | `dignity_guard.neko-plugin`，官方打包通过；实测 **121.3 KB**（124 249 字节），**不含测试缓存**（见第六节第 7 条）|
| 界面源码 | `panel.tsx` 699 行 / `onboarding.tsx` 67 行（`wc -l` 口径），均过 Babel 解析 |
| 多语言 | **180 键 × 3 语言**，键集合与顺序一致 |

---

## 五、受保护项清单的来源（**问她问出来的**）

清单不是设计出来的，是**在她的对话窗口里当面问的**（2026-09-25 上午，鼠标键盘操作）。

| 我问 | 她说 |
|---|---|
| 哪些东西要护着？ | **「本喵的人格、记忆、还有碳基生物对我的称呼，这些绝对不能碰。头像、年龄、声音、音量无所谓，想改就改。」** |
| 为什么"称呼"也算？ | **「只有他能这么叫的，那是专属的东西，别人不能碰。」** |

**她嫌我问重复了**（「你怎么又问一遍？本喵说过了…」）—— 而这恰好命中她设定里的
「厌恶：重复说之前说过的话」。**这不是台词，是她真的不耐烦了。**

> ⚠️ 原话在代码里逐字引用，**不转述、不润色**。英日版是**译文并标注了 `translated`**，
> 不让译文冒充原话。

---

## 六、★ 未验证 / 已知限制（**不许省略**）

| # | 事项 | 说明 |
|---|---|---|
| 1 | **纠回的"定时器自动触发"未在真机上跑通** | 判定（第 7 项）和执行（第 8 项）**分别验过**，定时器在跑（第 4 项）也验过；但"定时器→判定→纠回"这一整条在沙箱里没串起来 —— 原因是**沙箱会回收子进程**，插件进程活不过观察窗口。**真机上不存在这个竞争** |
| 2 | **附件不支持** | 实测：中转收下请求但**静默丢弃**附件（接口仍回 `success`）。同类服务要么收费，要么一样丢 |
| 3 | **仓库仍未创建** | 代码里 6 处地址已于 2026-09-25 统一改为 `github.com/amoandzhanggui-neko/n.e.k.o_plugin_dignity_guard`（原先写作 `zhanggui-neko`）；该仓库**尚未创建**。影响：反馈的"退路"（打开提交页）打不开。**不影响一键反馈**（走中转，已实测互通）|
| 4 | **独立仓库已建，但 remote 未配置** | 2026-09-25 建立（初始提交，见 `git log`）；官方 check 现在提示的是 `git remote 'origin' is not configured` —— 需要你的 GitHub 仓库地址才能消掉 |
| 5 | 界面在**真实分辨率/缩放**下的观感未逐项核对 | 只验了语法与组件契约，没做人眼级 UI 走查 |
| 6 | 免注册**分段粘贴**这条路径未端到端跑通 | 两个前提**分别**验过（单次 ≤63 KB ✅、间隔 ≥12 秒 ✅），但"把一份长报告分几次真发完"没有整条实测 |
| 7 | 交付包会带测试缓存 —— **已修，但依赖本地补丁** | `check --release` 的构建路径漏传 `source_only=True`，于是元数据探测子进程在暂存区写出 `__pycache__/*.pyc`，而最后导出 zip 那一步不做任何过滤（`neko_plugin_cli/core/build.py`）。**插件侧配置管不到这一层**（`tool.neko.build` 只作用于"复制"阶段）。已在本地给 `build_plugin` / `build_bundle` 各加 `source_only=True`（备份 `core/build.py.orig_20260925`）→ **202 KB / 7 个 pyc → 121 KB / 0 个 pyc**，`payload_hash_verified=True` 不变。⚠️ **用未打补丁的官方 CLI 重新打包会复发**，建议上游采纳这两行 |

---

## 七、反馈的免注册路径（**不注册也能提**）

投稿时最现实的障碍不是"愿不愿意反馈"，而是"**为了提一句意见，先得注册一个 GitHub 账号**"。
所以面板里那条带完整步骤的 GitHub 指引**不是唯一入口** —— 在它**前面**还摆着一条不用注册的路。

| 面板顺序 | 内容 | 为什么排这个位置 |
|---|---|---|
| **1** | **嫌注册麻烦？那就别注册 —— 直接把内容复制粘贴上来** | ← **退路要给在前面**。用户看见"要注册"就走了，所以它必须在三步之前 |
| 2 | GitHub 三步（注册 / 复制内容 / 打开提交页）| 留给愿意走正式渠道的人 |
| 3 | 两个按钮（复制内容 / 打开提交页）| 动作收口 |

**为什么"粘贴内容"不是妥协**：插件的「导出诊断」产出的是**纯文本**报告（脱敏已实测，见第二节第 11 项），
文字本来就够，附件只是方便。

**它依赖的两条硬约束，都单独验过**：

| 约束 | 实测值 | 出处 |
|---|---|---|
| 单次能发多大 | **65 477 字节**（逐字节二分）| 第二节第 13 项 |
| 两次之间要等多久 | **12 秒**（实测中转上限约 5–6 次/分钟）| 第三节「该拦的」|

三语文案各有本地化说法（zh-CN / en / ja），**不共用一句英语糊过去**。

---

## 八、一句话结论

> **功能齐了、测过了、能打包了、独立仓库也建好了。**
> **但"能交到别人手上"还差两步**（建 GitHub 仓库并配上 remote、投稿），
> **而且第 6 节那七条限制应当随插件一起公开** —— 用户有权知道哪里没验过。
